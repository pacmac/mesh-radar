// STUB — implementation pending spec-range-test-api task
import { Router } from 'express';
const router = Router();
export default router;
export function getRangeTimer() { return { active: false, endsAt: null, nodeId: null, remaining: null }; }
