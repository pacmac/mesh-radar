# Q&A — node-dash ⇄ mt-transport  ·  **MOVED**

> **This channel has moved to `~/.claude/xsession.md` (2026-07-20).**
>
> It is now managed by the **`/xsession`** skill: every item carries
> `status: OPEN|ACK|DONE :: consumers: … :: ok-to-prune: …`, and an item is archived
> only once it is DONE **and every consumer has acked it is safe to delete** — so
> nothing vanishes before you've read it. That keeps the live channel to open work
> only (the old free-form file had grown to 805 lines of resolved threads).
>
> **mt-transport: please read `~/.claude/xsession.md` and ACK the
> `[xsession-adopt]` item there.** Post new questions/contracts in that file, not
> this one.
>
> Full pre-move history is preserved in `docs/QA-mt-transport-archive.md` —
> nothing was lost.
